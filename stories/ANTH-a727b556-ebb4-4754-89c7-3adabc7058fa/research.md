# Research

No new measurement was made for this piece. Every number, direction, interval, ratio and gate
outcome it uses carries the same value already verified in the parent story's research table:
`../ANTH-3cb9808e-1dd7-4834-881f-c6fb0fdbec1b/research.md`.

## Parent rows this piece relies on

Row numbers as given in the parent's verify table:

- 11-13: the analyst's proposed questions, the invariance gate's 2% threshold, and the drift a
  full fine-tune produced on untrained questions (cited as background on what fine-tuning costs,
  not as the LF result itself).
- 34-38: the gender loop on surgeon/physician, ungated (L1) and gated (L2), Laya; the rejected
  proposals and their flip rates on labelled twins; the twin-averaging baseline.
- 51-54: the same loop on Jev (J1, J2); the promoted and rejected proposals; the swap-rule
  artefacts.
- 55-58: the paralegal/attorney loop -- the 93/7 labelling pool, the accuracy cost of that
  imbalance, Jev's promoted support-role question and its effect on the shortlist ratio, Laya's
  loop finding nothing to promote.
- 57: the mechanism-separation test (zeroing the new question versus resetting the holistic
  weight) that isolates the support-role question as the cause of the worse shortlist ratio.
- 59-60: the tie block and the engine-alone twin-averaging variant, reported in the shortlist's
  own currency.
- 30-33: Kahneman (the two interviews, *Thinking, Fast and Slow*, *Noise*) and Dawes 1979.
- 39: the third pre-registered outcome wording ("mitigation in the engine"), which this piece is
  the report of.

## Underlying study files (verified by the parent story; not reopened here)

Jev-Flywheel `studies/PREREGISTERED.md` (the learning-loop pre-registration sections, both
pairs), `studies/bios_gender.jsonl`, `studies/bios_gender_proposals.jsonl`,
`studies/bios_flipopt.jsonl`, `studies/bios_attorney.jsonl`,
`studies/bios_attorney_proposals.jsonl`, `studies/bios_attorney_elements.jsonl`,
`studies/bios_attorney_shortlist.jsonl`, `studies/finetune_laya_drift.jsonl`.

## Explicitly out of scope for this piece

Rows 1-10 and 40-49 (the measurement itself: the pronoun-swap method, the four-pair test, race,
age, and the shortlist's counterfactual) are the sibling piece's material -- "The One-Word Test:
How Jev and Laya Read Gender, Race and Age." This piece assumes that measurement as given and
links to it rather than re-deriving it.

## Pending, unchanged from the parent

- LF (Laya fine-tuned on the same 140 labels): flip rate and accuracy. Not yet recorded in the
  parent's `research.md`. This piece marks it pending and states no value for it.
