# Decision Models Are Not Calculators

Copywriting draft lives in [anthus-site-content PR #16](https://github.com/AnthusAI/anthus-site-content/pull/16)
as `decision-models-are-not-calculators.mdx`, with the response-derived social
cover and two detailed figures under `images/`. This story packet deliberately
links to that single source of publishable copy instead of keeping a second
version that can drift.

The matching Gatsby submodule pin is in [Anth.us PR #62](https://github.com/AnthusAI/Anth.us/pull/62)
against `develop`; its isolated production build passed. Neither review PR
has been merged.

Status: awaiting Ryan's editorial approval. The MDX frontmatter is
`state: draft`; do not merge or deploy until approved.

The public evidence is [Decision Models Are Not Calculators](https://github.com/AnthusAI/Decision-Models-Are-Not-Calculators).
It includes all 8,640 replies, preregistration, checksummed archives,
reproducible summaries, example requests/responses, and the figures used in
the copy. The draft says what the three models did without attributing their
behavior to Benford's Law or their uninspected training data.

Limatus scanned the draft against `publications/anthus/style-profile.yml`;
the [diagnosis](limatus-diagnosis.json) reports 1,490 words and density within
the profile thresholds. Remaining automatic flags concern the requested
Title Case headline, the exact quoted prompt's passive construction, MDX alt
text, and numeric claims whose attribution is the linked released summary.
These were reviewed against the source records; they are not unsupported
claims in the article.
